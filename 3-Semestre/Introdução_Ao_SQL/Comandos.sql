-- Comandos em SQL

SELECT * (todas as tabelas)
SELECT X = qualquer nome (dá um apelido),(SELECT X AS qualquer nome tambem serve)

UPDATE X = (atualiza coluna)

SET X = X + 1 (define um valor a algo)

FROM = qualquer tabela (busca os dados)

INSERT = (inserir atributo na tabela por código)

ORDER BY = (ordenar por)

WHERE = (busca o dado do número que digitar)

OR = (um ou outro)

COUNT = (conta quantos elementos tem na tabela)
SELECT TOTAL = COUNT(*)

AVG = (média aritmética)

CREATE PROCEDURE qualquer nome = (criar programa)
AS BEGIN
END

ALTER PROCEDURE qualquer nome = (alterar um processo já criado)
AS BEGIN
END
